server:
  port: 8761
eureka:
  client:
    register-with-eureka: false  # ·ÀÖ¹×Ô¼º×¢²á
    fetch-registry: false
    serviceUrl:
       defaultZone:  http://${eureka.instance.hostname}:${server.port}/eureka/
  instance:
    hostname: localhost
